sprint:
  id: sprint-2026-03-08-test-coverage
  name: "Test Coverage Expansion — Utilities & Stores"
  goal: goal-12
  status: ready
  created: 2026-03-08
  estimated_hours: 2.0
  description: |
    Add test files for 7 untested utility modules to bring test file count from 28 to 35+
    and improve test-to-source ratio from 0.12 toward 0.20+.
    Pure test additions — no source changes. All 929 existing tests must continue to pass.
    Auto-generated by autopilot for goal-12.
  exit_criteria:
    - "Test files exist for stageHelpers, unifiedExporter, and analyticsCalculations"
    - "Test files exist for costCalculations, batchNumbering, motherPlantMetrics, and exporters"
    - "Total test file count is 35 or more"
    - "All tests pass (929+ existing + new tests)"
    - "Build succeeds"
  handoffs:
    - id: tc-001-util-tests
      title: "Create Tests for Core Propagation Utilities"
      priority: P0
      estimate_hours: 1.0
      description: |
        Create test files for 4 propagation utility modules:

        1. src/modules/propagation/utils/__tests__/stageHelpers.test.ts
           - Test STAGE_DISPLAY_NAMES mapping completeness
           - Test stage transition validation (valid transitions, invalid transitions)
           - Test days-in-stage calculations
           - Test stage color/icon helpers
           Target: 8+ tests

        2. src/modules/propagation/utils/__tests__/analyticsCalculations.test.ts
           - Test analytics barrel re-exports
           - Test success rate calculations (with data, empty data, edge cases)
           - Test cost analytics calculations
           - Test timeline/trend analysis functions
           Target: 10+ tests

        3. src/modules/propagation/utils/__tests__/costCalculations.test.ts
           - Test getTotalBatchCost with supply-linked and manual costs
           - Test per-propagule cost calculation
           - Test cost breakdown by category
           - Test edge cases: empty costs, zero quantities
           Target: 8+ tests

        4. src/modules/propagation/utils/__tests__/batchNumbering.test.ts
           - Test parseBatchNumber (valid, invalid formats)
           - Test formatBatchNumber (padding, year handling)
           - Test getNextBatchNumber (with existing batches, fresh year)
           Target: 6+ tests

        Follow existing test patterns in src/modules/propagation/stores/__tests__/.
        Use vitest (describe/it/expect). Mock db calls where needed.

        IMPORTANT: Run `pnpm vitest run` after creating each test file.
      acceptance_criteria:
        - "stageHelpers.test.ts exists with 8+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/stageHelpers.test.ts exits 0)"
        - "analyticsCalculations.test.ts exists with 10+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/analyticsCalculations.test.ts exits 0)"
        - "costCalculations.test.ts exists with 8+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/costCalculations.test.ts exits 0)"
        - "batchNumbering.test.ts exists with 6+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/batchNumbering.test.ts exits 0)"
      blocked_by: []

    - id: tc-002-exporter-metric-tests
      title: "Create Tests for Exporters and Metrics"
      priority: P0
      estimate_hours: 0.75
      description: |
        Create test files for 3 remaining untested modules:

        1. src/lib/utils/__tests__/unifiedExporter.test.ts
           - Test unified export format structure
           - Test data aggregation across Grow + Propagation modules
           - Test import/restore validation
           - Mock db calls (propDb, growDb)
           Target: 8+ tests

        2. src/modules/propagation/utils/__tests__/motherPlantMetrics.test.ts
           - Test ExtendedMotherPlantMetrics calculation
           - Test success rate computation
           - Test method breakdown aggregation
           - Test edge cases: no batches, all failed
           Target: 6+ tests

        3. src/modules/propagation/utils/__tests__/exporters.test.ts
           - Test propagation-specific export functions
           - Test CSV/JSON formatting
           - Test data filtering and transformation
           Target: 6+ tests

        Follow existing test patterns. Use vitest. Mock IndexedDB/db calls.

        IMPORTANT: Run `pnpm vitest run` after creating each test file.
      acceptance_criteria:
        - "unifiedExporter.test.ts exists with 8+ tests (verified via: pnpm vitest run src/lib/utils/__tests__/unifiedExporter.test.ts exits 0)"
        - "motherPlantMetrics.test.ts exists with 6+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/motherPlantMetrics.test.ts exits 0)"
        - "exporters.test.ts exists with 6+ tests (verified via: pnpm vitest run src/modules/propagation/utils/__tests__/exporters.test.ts exits 0)"
        - "Total test file count is 35+ (verified via: find src -name '*.test.*' -o -name '*.spec.*' | wc -l shows >= 35)"
      blocked_by: []

    - id: tc-999-commit-verify
      title: "Commit and Verify CI"
      priority: P0
      estimate_hours: 0.25
      description: |
        Final sprint handoff - commit, push, verify CI, update goals.

        1. git status - review changes
        2. git add - stage files (avoid secrets)
        3. git commit - descriptive message
        4. git push - push to remote
        5. Verify CI passes (all checks green)
        6. Update goal progress in .claude/project.yaml

        DO NOT mark sprint complete until CI is green.
      acceptance_criteria:
        - "All changes committed (verified via: git status --porcelain shows clean)"
        - "Pushed to remote (verified via: git push exits 0)"
        - "Build passes (verified via: pnpm run build exits 0)"
        - "Tests pass (verified via: pnpm vitest run exits 0)"
        - "Goal progress updated (verified via: grep 'progress:' .claude/project.yaml)"
        - "All prior handoff acceptance criteria pass (verified via: /project:sprint-validate --acceptance exits with verdict PASS)"
      blocked_by: ["tc-001-util-tests", "tc-002-exporter-metric-tests"]

retro:
  conducted_at: "2026-03-08T01:30:49Z"
  duration: "~8m"
  completion_rate: 100
  estimation_variance: -93
  acceptance_verification:
    total_criteria: 14
    passed: 14
    failed: 0
    manual: 0
    verdict: "PASS"
    criteria_quality_pct: 100
  successes:
    - "All 7 test files created and passing on first attempt"
    - "Test count exceeded targets: 151 new tests (929→1080, +16%)"
    - "35 test files achieved (was 28), meeting goal-12 sc-12-3"
    - "Build remained green throughout"
  challenges: []
  action_items:
    - "Reduce estimates for pure test-creation sprints — AI agents are much faster"
    - "Test-to-source ratio at 0.14, still below 0.20 target — may need another sprint"
  recommendations:
    - "Target component-level or integration tests next for deeper coverage"
    - "Consider testing UI components for higher ratio impact"
